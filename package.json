{
  "name": "plas-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "server": "node --env-file=Key.env server.js",
    "start": "node --env-file-if-exists=Key.env server.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=20.12"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
